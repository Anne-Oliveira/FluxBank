<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundLight"
    tools:context=".ConfirmarPagamentoActivity">

    <!-- Header -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/home_header_gradient"
        app:elevation="0dp"
        app:layout_constraintTop_toTopOf="parent">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize">

            <ImageView
                android:id="@+id/btnBack"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/ic_arrow_left"
                app:tint="@android:color/white"/>

            <TextView
                android:id="@+id/header_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="Confirmar Pagamento"
                android:textColor="@android:color/white"
                android:textSize="20sp"
                android:textStyle="bold" />
        </androidx.appcompat.widget.Toolbar>
    </com.google.android.material.appbar.AppBarLayout>

    <!-- Content -->
    <ImageView
        android:id="@+id/recipient_icon"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:background="@drawable/circular_background"
        android:padding="12dp"
        android:src="@drawable/ic_user_profile"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/appBarLayout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <TextView
        android:id="@+id/payment_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="R$ 0,01"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@+id/recipient_icon"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <TextView
        android:id="@+id/recipient_name_big"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Anee Oliveira"
        app:layout_constraintTop_toBottomOf="@+id/payment_value"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Payment Type -->
    <TextView
        android:id="@+id/payment_type_label"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Agora\n04/12/2025"
        android:padding="12dp"
        android:background="@drawable/edit_text_background"
        android:layout_marginTop="24dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintTop_toBottomOf="@+id/recipient_name_big"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Details Section -->
    <TextView
        android:id="@+id/receiver_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Quem vai receber"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@+id/payment_type_label"
        app:layout_constraintStart_toStartOf="@+id/payment_type_label"/>

    <TableLayout
        android:id="@+id/details_table"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/receiver_title"
        app:layout_constraintStart_toStartOf="@id/receiver_title"
        app:layout_constraintEnd_toEndOf="@id/payment_type_label">

        <TableRow>
            <TextView android:text="Nome:" android:layout_marginEnd="8dp"/>
            <TextView android:id="@+id/recipient_name" android:text="Anee Oliveira" android:textStyle="bold"/>
        </TableRow>
        <TableRow>
            <TextView android:text="CPF:"/>
            <TextView android:id="@+id/recipient_cpf" android:text="***.111.222-**" android:textStyle="bold"/>
        </TableRow>
        <TableRow>
            <TextView android:text="Instituição:"/>
            <TextView android:id="@+id/recipient_institution" android:text="Banco Bradesco" android:textStyle="bold"/>
        </TableRow>
        <TableRow>
            <TextView android:text="Chave:"/>
            <TextView android:id="@+id/recipient_key" android:text="333.111.222-12" android:textStyle="bold"/>
        </TableRow>
    </TableLayout>

    <Button
        android:id="@+id/btnPagar"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:text="Pagar R$0,01"
        android:background="@drawable/button_gradient_background"
        android:textColor="@android:color/white"
        app:backgroundTint="@null"
        android:layout_margin="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>